spring:
  application:
    name: api_rest_java_with_spring_boot

  datasource:
    url: jdbc:postgresql://localhost:5432/api_rest_java_with_spring_boot
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
